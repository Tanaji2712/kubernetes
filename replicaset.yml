apiVersion: apps/v1
kind: ReplicaSet
metadata:
  name: nginx-rs
  namespace: workshop

spec:
  replicas: 3
  selector:
    matchLabels:
      name: nginx

  template:
    metadata:
      labels:
        app: nginx

    spec:
      containers:
        - name: nginx
          image: nginx:1.25
