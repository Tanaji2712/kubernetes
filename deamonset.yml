apiVersion: apps/1
kind: DeamonSet
metadata:
  name: fluentd
  namespace: workshop

spec:
  selector:
    matchlabels:
      app: fluentd

  template:
    matadata:
      labels:
        app: fluentd

    spec:
      containers:
      - name: fluentd
        image: fluentd
